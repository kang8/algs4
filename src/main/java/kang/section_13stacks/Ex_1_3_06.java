package kang.section_13stacks;

/**
 * What does the following code fragment do to the queue q?
 * <pre>{@code
 * Stack<String> stack = new Stack<String>();
 * while(!q.isEmpty())
 *      stack.push(q.dequeue());
 * while(!stack.isEmpty())
 *      q.enqueue(stack.pop());
 * }</pre>
 *
 * @Answer: reverse queue sequence.
 * 反转 queue 的序列
 */
public class Ex_1_3_06 {
}
